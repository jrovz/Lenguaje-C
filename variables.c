#include <stdio.h>

// Declaración de variables.
int a, b, c;

float f, g, h;

int main()
{
    // Inicialización de variables.
    a = 2;
    b = 458;
    c = a + b;
    printf("El valor de la suma es: %d", c);
    return 0;
}